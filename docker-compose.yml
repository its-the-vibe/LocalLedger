services:
  app:
    build: .
    image: ghcr.io/its-the-vibe/localledger:latest
    pull_policy: always
    ports:
      - "${PORT:-8080}:8080"
    volumes:
      - ./dist:/dist:ro
    environment:
      - LISTEN_ADDR=:8080
    read_only: true
    restart: on-failure:10
